# Production dependencies. torch is installed separately from the CPU index in
# the Dockerfile, so it is deliberately absent here.

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-multipart>=0.0.9
SQLAlchemy>=2.0
httpx>=0.27
Pillow>=10.0
numpy>=1.24,<2.0

# Speech recognition. Pulls tiktoken and numba; torch is already present.
openai-whisper==20240930

# The OpenCV geometry pass. headless: no GUI libraries on a server.
opencv-python-headless>=4.9

# Bundled ffmpeg for video/compressed-audio ingest — nothing to apt-install.
imageio-ffmpeg>=0.5
